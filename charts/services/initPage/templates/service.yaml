apiVersion: v1
kind: Service
metadata:
  name: {{ include "initpage.fullname" . }}
  labels:
    {{- include "initpage.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "initpage.selectorLabels" . | nindent 4 }}
