apiVersion: v1
kind: Service
metadata:
  name: {{ include "team04-fe.fullname" . }}
  labels:
    {{- include "team04-fe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "team04-fe.selectorLabels" . | nindent 4 }}
